name: Sync Financial Disclosure PDFs

on:
  schedule:
    - cron: '0 2 * * *'   # Run every day at 02:00 UTC
  workflow_dispatch:       # Allow manual trigger

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout repo
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      # Step 2: Ensure latest code
      - name: Ensure latest code
        run: git pull --rebase

      # Step 3: Configure Git identity
      - name: Configure Git user
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      # Step 4: Download and unzip latest financial data
      - name: Fetch and unzip financial PDFs
        run: |
          set -euo pipefail
          ZIP_URL="https://disclosures-clerk.house.gov/public_disc/financial-pdfs/2026FD.zip"
          DEST_ZIP="2026FD.zip"
          OUT_DIR="financial-pdfs"

          echo "Downloading from $ZIP_URL ..."
          curl -L --fail "$ZIP_URL" -o "$DEST_ZIP"
          mkdir -p "$OUT_DIR"
          unzip -o "$DEST_ZIP" -d "$OUT_DIR" > /dev/null
          rm "$DEST_ZIP"

      # Step 5: Set up Python environment
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # Step 6: Set up Chrome (cleaner than apt-get)
      - name: Set up Chrome
        uses: browser-actions/setup-chrome@v1

      # Step 7: Install Python dependencies
      - name: Install dependencies
        run: pip install selenium

      # Step 8: Run your Python PDF downloader
      - name: Run PDF downloader
        run: python scripts/dl_pdfs.py

      # Step 9: Commit and push any new PDFs
      - name: Commit and push changes
        run: |
          if [ -n "$(git status --porcelain)" ]; then
            echo "Changes detected — committing and pushing."
            git add .
            git commit -m "Automated update: synced new 2025 PDFs"
            git push
          else
            echo "No changes to commit."
          fi
